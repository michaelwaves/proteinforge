{
  "permissions": {
    "allow": [
      "Bash(find /workspace/RFdiffusion/examples -name *.txt -o -name *.yml -o -name *.yaml)",
      "Bash(nvidia-smi --query-gpu=name,memory.total,driver_version,compute_cap --format=csv,noheader)",
      "Bash(uv --version)",
      "Bash(python3 -c \"import torch; print\\(f''torch={torch.__version__}, cuda={torch.cuda.is_available\\(\\)}, device={torch.cuda.get_device_name\\(0\\) if torch.cuda.is_available\\(\\) else \"\"N/A\"\"}''\\)\")",
      "Bash(python3 -c \"import dgl; print\\(dgl.__version__\\)\")"
    ]
  }
}
